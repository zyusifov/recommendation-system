## Fast Api microservice with recommendation-system to get a list of objects

Microservice for predict a list of objects on `Fast Api` using a `scikit-learn` library for python.<br>
Deploy with `docker`
